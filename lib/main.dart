import 'package:flutter/material.dart';
import 'package:nike_sneakers_store_app/profile.dart';
import 'home.dart';

void main() {
  runApp(Profile());
  // runApp(HomePage());
  // runApp(SplashScreen());
}
